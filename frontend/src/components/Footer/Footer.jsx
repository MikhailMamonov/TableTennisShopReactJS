import React    from "react";
import classes from "./Footer.module.css";

const Footer = (props) => {
    return   (<footer className={classes.footer}>
      <h5>Copyright Михаил Мамонов</h5>
      </footer>) 
  }

export default Footer;
